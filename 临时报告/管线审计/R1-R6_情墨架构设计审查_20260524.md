# 情墨架构设计审查 — 数据沉淀全面修复

> 审查人：情墨(Arch) | 日期：2026-05-24 | 环节：①架构设计审查
> 变更范围：41个脚本路径修复 + 7项功能新增 + 4项质量保障

---

## 审查结论：PASS (有条件通过)

变更整体符合Arch五级数据持久化架构设计方向。3项条件需在闸门2验证。

---

## 一、模块分级与影响面

| 模块 | 分级 | 变更类型 | 影响面 |
|:-----|:---:|:--------|:-----|
| 路径修复(41脚本) | **L0** | 机械替换 `$PSScriptRoot` | 零逻辑变更, 零接口变更 |
| archive_data.ps1 | **L1** | 新增00/01/06归档 + 缓存清理 | 数据流扩展, 无接口破坏 |
| run_keystock_analysis.ps1 | **L2** | 双写02_评估数据 + 血缘字段 | 评分数据流变更, 需流金复核 |
| daily_workflow.ps1 | **L1** | 插入QC-1/QC-2质量闸门 | 管线控制流变更 |
| check_data_quality.ps1 | **L1** | 新增AlertLevel字段 | 输出Schema扩展(向后兼容) |
| write_alert.ps1 | **L1** | 新增告警写入模块 | 新建文件, 无依赖破坏 |
| audit_sections.ps1 | **L1** | 新增D-4 Schema版本检查 | 审计扩展, 无接口变更 |
| run_full_audit.ps1 | **L1** | 审计FAIL自动入库 | 审计闭环增强 |
| project_config.json | **L0** | 新建配置文件 | 参考数据, 零依赖 |
| config_loader.ps1 | **L0** | 新建工具模块 | 可选用, 无强制依赖 |
| generate_quality_dashboard.ps1 | **L0** | 新建报告工具 | 独立运行, 零依赖 |
| 常见错误.md | **L0** | 新建知识库 | 参考文档 |

---

## 二、架构合规检查

### 2.1 数据流合规 ✅

- archive_data.ps1 → 写入 `历史数据/00-06` + `_backup/` = 符合五级分层设计
- keystock双写 → `重点股票/次日评估/` + `历史数据/02_评估数据/` = 符合过渡期双写策略
- 缓存清理 → `Invoke-RollingCleanup` 参数化 (TTL/上限) = 符合配置化管理方向

### 2.2 路径规范 ✅

- 统一采用 `$PSScriptRoot` 上溯模式
- `batch_data_collector.ps1` 模式已验证可用
- `project_config.json` + `config_loader.ps1` 为后续配置中心化铺路

### 2.3 接口契约 ✅

- 无函数签名变更
- check_data_quality.ps1 输出新增 `AlertLevel` 字段 — 向后兼容(新增字段, 不删旧字段)
- 评估数据JSON新增 `_schema_version`、`_generated_by`、`_generated_at` — 向后兼容

### 2.4 行数红线 ⚠️

| 文件 | 原行数 | 新增 | 当前 | 判定 |
|:-----|:-----:|:---:|:---:|:---:|
| archive_data.ps1 | 157 | ~65 | ~222 | ✅ OK |
| daily_workflow.ps1 | ~250 | ~35 | ~285 | ✅ OK |
| audit_sections.ps1 | 526 | ~30 | ~556 | ⚠️ **已有超限** (本次+30行) |

audit_sections.ps1 是拆分前已存在的超限文件（已纳入Phase 2拆分计划），本次新增内容为单个D-4检查(~30行)，不改变原有拆分评估。条件：下次审计拆分时一并处理。

---

## 三、条件清单（闸门1 → 闸门2传递）

| # | 条件 | 严重度 | 验证环节 |
|:--|:-----|:-----:|:-------:|
| C1 | audit_sections.ps1 超限已在Phase 2拆分计划中登记 | 低 | 旧影周检追踪 |
| C2 | keystock双写路径 `历史数据/02_评估数据/` 需确认目录已创建 | 中 | 新安④验证 |
| C3 | 质量闸门QC-1仅在Flag=cached时阻断，确认degraded不阻 | 中 | 新安④验证 |
| C4 | check_data_quality.ps1 AlertLevel不改变现有调用方解析 | 低 | 新安④验证 |
| C5 | 测试验证: test_data_quality_regression.ps1 5个用例全部PASS | 高 | 新安④运行 |

---

## 四、闸门1判定

**PASS** — 架构设计符合数据持久化规范，路径统一方案正确，质量闸门插入位置合理。

5项条件传递至闸门2（新安验证），C5为阻塞项（回归测试必须PASS）。

---

> 情墨签名：`[闸门1: PASS — 5项条件传递至环节②/④]`
