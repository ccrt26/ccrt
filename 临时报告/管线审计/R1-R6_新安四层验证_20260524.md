# R1-R6 数据沉淀全面修复 -- 新安四层验证报告

> **审查日期**：2026-05-24
> **审查类型**：完整审查（上线前闸门2验证）
> **审查人**：新安（Proof）
> **变更范围**：R1-R6 六轮修复，41个脚本路径修复 + 归档架构重构 + 质量闸门 + 告警模块 + 配置中心
> **前置条件**：闸门1 PASS（情墨设计审查通过，5项条件满足）

---

## 一、审查结论

| 层级 | 名称 | 结果 | 关键发现 |
|:----:|:-----|:----:|:---------|
| L1 | 变更影响分析 | **PASS** | 4个关键文件全部符合设计规格 |
| L2 | 测试验证 | **QUALIFIED PASS** | 回归测试代码逻辑正确，环境编码问题阻断执行 |
| L3 | 回归检查 | **PASS** | 硬编码路径零残留，关键JSON使用UTF8NoBOM |
| L4 | 红线审查 | **CONDITIONAL PASS** | 未触评分/风控/PDF红线；1项B1遗留（非本次引入） |

- 检查条款总数：17
- 通过：14
- 发现问题：3（B1=1, B2=2）
- **总体结论**：**闸门2: PASS（含条件）**

---

## 二、第一层：变更影响分析

### 2.1 daily_workflow.ps1 -- QC-1 阻断条件

**文件**：`代码文件/每日荐股/scripts/daily_workflow.ps1`（328行）

**审查点**：确认QC-1仅在Flag=cached时阻断（非degraded）

**审查结果**：**PASS**

```powershell
# Line 201: 阻断条件
if (-not $qcJson1.Passed -or $qcJson1.Flag -eq "cached") {
```

阻断逻辑分析：

| Flag值 | check_data_quality 中 Passed 状态 | 阻断判定 | 是否正确 |
|:-------|:----------------------------------|:--------|:--------:|
| normal | true | 不阻断 | YES |
| degraded（纯降级，无ERROR） | true | 不阻断 | YES |
| degraded + ERROR（如null字段） | false | 阻断 | YES（ERROR应阻断）|
| cached（双源全挂，数据OK） | true | 阻断 | YES（符合设计意图）|
| cached + ERROR（双源全挂+数据问题） | false | 阻断 | YES |

QC-2（Line 235）使用更宽松的条件 `(-not $qcJson2.Passed)`，仅阻断有ERROR的情况。与QC-1形成分层防御。正确。

**结论**：QC-1精确实现了"缓存态阻断、降级态放行（无ERROR时）"的设计意图。

### 2.2 archive_data.ps1 -- S级双副本SHA256校验

**文件**：`代码文件/每日荐股/scripts/archive_data.ps1`（238行）

**审查点**：确认S级双副本SHA256校验逻辑完整

**审查结果**：**PASS**

校验逻辑（Lines 170-194）：
1. 创建 `历史数据/00_核心交易` 和 `历史数据/_backup` 两个目录
2. 遍历3个S级资产（transactions.csv, positions.json, perf_summary.json）
3. 每个资产 Copy-Item 到两个目录
4. 对两副本分别计算 SHA256：`(Get-FileHash $dstCore -Algorithm SHA256).Hash` / `(Get-FileHash $dstBackup -Algorithm SHA256).Hash`
5. 若哈希不一致 → 记录 ERROR 级别日志
6. 一致 → 记录"SHA256校验通过"

新增归档目录覆盖完整：
- `00_核心交易` + `_backup`（S级双副本）
- `01_交易快照`（交易快照归档）
- `02_评估数据`（评估数据归档）
- `04_原始数据` / `05_参考数据`（已有目录，Trim-ToLatest + Clean-OldArchives）
- `06_月度归档`（月度文件归档）
- 缓存清理（Invoke-RollingCleanup, Lines 85-109）

**结论**：SHA256双副本校验完整，00/01/02/04/05/06 全目录覆盖，缓存滚动清理生效。

### 2.3 run_keystock_analysis.ps1 -- 双写路径与BOM修复

**文件**：`代码文件/重点股票/run_keystock_analysis.ps1`（1985行）

**审查点**：确认双写路径和BOM修复

**审查结果**：**PASS**

双写路径（Lines 1963-1968）：
```powershell
# 主路径：重点股票/次日评估/评估数据_${dateStr}.json
$evalFile = Join-Path $evalDir "评估数据_${dateStr}.json"
[System.IO.File]::WriteAllText($evalFile, $evalJson, [System.Text.UTF8Encoding]::new($false))

# P0-1: 双写到历史数据/02_评估数据/ (情墨持久化架构)
$archiveEvalFile = Join-Path $archiveEvalDir "评估数据_${dateStr}.json"
Copy-Item $evalFile $archiveEvalFile -Force
```

BOM修复确认：

| 行号 | 方法 | BOM |
|:----:|:-----|:---:|
| 1855 | `[System.Text.UTF8Encoding]::new($false)` | 无BOM |
| 1960 | `[System.Text.UTF8Encoding]::new($false)` | 无BOM |

v3.0字段完整性校验（Lines 1970-1985）：验证 `ADX_Value, ADX_Trend, OBV_Trend, Wyckoff_Phase` 四个必填字段存在，缺失时输出WARNING。

**结论**：双写路径正确，BOM已修复，v3.0字段校验完整。

### 2.4 check_data_quality.ps1 -- AlertLevel判定逻辑

**文件**：`代码文件/tools/check_data_quality.ps1`（172行）

**审查点**：确认AlertLevel判定逻辑和BOM修复

**审查结果**：**PASS**

AlertLevel判定逻辑（Lines 162-168）：

| Flag | Passed | AlertLevel | 语义 |
|:-----|:------:|:----------:|:-----|
| normal | 任意 | L0 | 正常 |
| degraded | 任意 | L1 | 轻微降级 |
| cached | true | L2 | 中度降级（双源全挂但数据OK） |
| cached | false | L3 | 严重（双源全挂+数据有问题） |

BOM：check_data_quality.ps1 本身通过 `ConvertTo-Json` 输出到stdout，不涉及文件写入BOM。其调用方（daily_workflow.ps1）通过 `ConvertFrom-Json` 解析，无BOM问题。

**结论**：AlertLevel四级判定逻辑清晰正确。

---

## 三、第二层：测试验证

### 3.1 test_data_quality_regression.ps1 执行

**文件**：`代码文件/tools/test_data_quality_regression.ps1`（121行）

**执行状态**：**QUALIFIED PASS**（环境限制，代码逻辑审查通过）

**原因**：从bash桥接调用PowerShell时，脚本的中文字符编码被破坏，导致PowerShell解析器报错。这不是脚本代码的问题——通过Read工具读取的脚本内容语法完整、逻辑正确。

**代码审查确认**（5个用例全部正确）：

| 用例 | 损坏方式 | 期望Flag | 期望Passed | 判定 |
|:-----|:--------|:--------:|:---------:|:----:|
| TC-1 null_TotalScore | TotalScore设为null | degraded | false | 逻辑正确 |
| TC-2 empty_recommendations | 推荐列表清空 | degraded | false | 逻辑正确 |
| TC-3 missing_file | 数据文件不存在 | cached | false | 逻辑正确 |
| TC-4 null_MA5 | MA5设为null | degraded | false | 逻辑正确 |
| TC-5 normal_data | 不改动 | normal | true | 逻辑正确 |

关键验证点：
- TC-1/TC-4 验证null值穿透检测（check: "null值穿透", Level=ERROR）
- TC-2 验证推荐数不足检测（check: "推荐股票数", Level=WARN）
- TC-3 验证文件缺失时直接标记cached
- TC-5 验证正常数据全部通过
- 测试使用 `[System.Text.UTF8Encoding]::new($false)` 写入损坏数据 → 确认UTF8NoBOM
- 测试后自动清理临时文件（Line 58, 120）

**建议**：建议红枫部署后在Windows原生PowerShell环境中运行一次回归测试以确认。

### 3.2 write_alert.ps1 告警模块

**文件**：`代码文件/每日荐股/scripts/write_alert.ps1`（31行）

**审查结果**：**PASS**

- 使用 `$PSScriptRoot` 路径，无硬编码
- 写入 `每日荐股/运营记录/issues.csv`，使用 `Export-Csv -Encoding UTF8 -Append`
- 函数签名清晰：Date, AlertLevel, Flag, DegradedFields, CachedFields, Detail
- 行数31，远低于500行限制

---

## 四、第三层：回归检查

### 4.1 全项目硬编码路径 C:\Users\34269 残留

**审查方法**：grep `C:\\Users\\34269` 在 `代码文件/` 目录下按文件类型分类

| 文件类型 | 匹配数 | 状态 | 说明 |
|:---------|:------:|:----:|:-----|
| **.ps1** | **0** | **PASS** | 所有PowerShell脚本已清理完毕 |
| .bat / .cmd | 4个文件 | ACCEPT | Task Scheduler调度必须使用绝对路径 |
| .xml | 1个文件 | ACCEPT | Windows Task Scheduler XML定义 |
| .log | ~50处 | IGNORE | 历史日志文件，运行时自动生成 |

**结论**：**PASS** — 41个脚本的 `$PSScriptRoot` 迁移验证通过，代码文件中零残留。

### 4.2 关键JSON写入UTF8NoBOM

**审查方法**：搜索 `WriteAllText` 和 `ConvertTo-Json` 后的编码参数

| 文件 | 行号 | 编码方法 | BOM | 状态 |
|:-----|:----:|:---------|:---:|:----:|
| run_keystock_analysis.ps1 | 1855 | `UTF8Encoding::new($false)` | 无 | PASS |
| run_keystock_analysis.ps1 | 1960 | `UTF8Encoding::new($false)` | 无 | PASS |
| test_data_quality_regression.ps1 | 38 | `UTF8Encoding::new($false)` | 无 | PASS |
| generate_quality_dashboard.ps1 | 88 | `UTF8Encoding::new($false)` | 无 | PASS |
| run_daily_eval.ps1 | 660 | `UTF8Encoding::new($false)` | 无 | PASS |

**遗留问题**（非R1-R6范围，标记为B2）：

| 文件 | 行号 | 编码方法 | BOM | 建议 |
|:-----|:----:|:---------|:---:|:-----|
| gen_daily_html.ps1 | 404 | `[System.Text.Encoding]::UTF8` | 有(BOM) | 改为 `UTF8Encoding::new($false)` |
| build_dynamic_pool.ps1 | 204 | `[System.Text.Encoding]::UTF8` | 有(BOM) | 改为 `UTF8Encoding::new($false)` |
| run_keystock_evaluation.ps1 | 1047 | `[System.Text.Encoding]::UTF8` | 有(BOM) | 改为 `UTF8Encoding::new($false)` |

### 4.3 predictions.csv 去重后格式

**文件**：`重点股票/预判记录/predictions.csv`

| 检查项 | 结果 |
|:-------|:-----|
| 总行数 | 9行（1 header + 8 data） |
| 重复行 | 无 |
| 分隔符 | 逗号 |
| 引号 | 双引号包裹所有字段 |
| 编码 | UTF-8 with BOM（`efbbbf`） |

**发现**：CSV文件带有UTF-8 BOM标记。Python `csv.reader` 解析时会将BOM合并到第一个列名中（`﻿"date"`）。建议后续写入时去除BOM。标记为B2。

---

## 五、第四层：红线审查

### 5.1 是否修改评分/交易/风控逻辑

**审查结果**：**PASS -- 未触及**

逐项确认：

| R1-R6 变更项 | 是否触及评分 | 是否触及交易 | 是否触及风控 |
|:------------|:----------:|:----------:|:----------:|
| 41个脚本路径修复 | 否 | 否 | 否 |
| archive_data.ps1 归档重构 | 否 | 否 | 否 |
| run_keystock_analysis.ps1 双写 | 否 | 否 | 否 |
| daily_workflow.ps1 QC闸门 | 否 | 否 | 否（质量监控，不修改评分）|
| check_data_quality.ps1 AlertLevel | 否 | 否 | 否（质检，非风控逻辑）|
| write_alert.ps1 告警模块 | 否 | 否 | 否（告警记录，非风控决策）|
| run_full_audit.ps1 审计增强 | 否 | 否 | 否 |
| project_config.json 配置中心 | 否 | 否 | 否 |
| generate_quality_dashboard.ps1 | 否 | 否 | 否 |

daily_workflow.ps1 仅调用 `scoring_engine_v2.py`（Line 214），未修改引擎本身。QC闸门增加的是质量前置检查，不干预评分/排序/否决逻辑。

### 5.2 是否违反单文件<=500行规则

**审查结果**：**B1 -- run_keystock_analysis.ps1 = 1985行（预存遗留，非本次引入）**

| 文件 | 行数 | 状态 |
|:-----|:---:|:----:|
| daily_workflow.ps1 | 328 | PASS |
| archive_data.ps1 | 238 | PASS |
| check_data_quality.ps1 | 172 | PASS |
| test_data_quality_regression.ps1 | 121 | PASS |
| generate_quality_dashboard.ps1 | 89 | PASS |
| write_alert.ps1 | 31 | PASS |
| **run_keystock_analysis.ps1** | **1985** | **B1** |

`run_keystock_analysis.ps1` 的1985行是预存状态，R1-R6仅新增约6行双写逻辑（Lines 1963-1968）。该文件在Phase 2轻量化计划中已标记为待拆分。

**裁决**：标记为B1-遗留，不阻塞本次闸门，但需在后续Phase纳入拆分计划。

### 5.3 是否删除任何PDF文件

**审查结果**：**PASS -- 无PDF删除**

搜索 `Remove-Item.*\.pdf` / `del.*\.pdf` / `rm.*\.pdf` 在 `代码文件/` 下：**0处匹配**。

archive_data.ps1 中的 `Remove-Item` 操作仅针对：
- 超过保留期的归档文件（>90天）
- 超过数量上限的归档文件（Trim-ToLatest）
- 超过TTL的缓存文件（>168小时）
- 以上操作均在 `历史数据/` 子目录中，不涉及项目PDF报告

### 5.4 其他红线条款快速检查

| 红线条款 | 审查结果 | 说明 |
|:---------|:--------:|:-----|
| §1.1 数据真实性 | PASS | 无硬编码数据，无数据编造 |
| §1.2 1+2架构 | PASS | 未修改数据源获取逻辑 |
| §1.3 禁止编造 | PASS | 无AI推断 |
| §1.6 CHANGELOG | PASS | 无虚假记录 |
| §2 Token纪律 | PASS | 未增加AI调用 |
| §3 API间隔 | PASS | 未修改API调用逻辑 |
| §5.4 版本管理 | PASS | 未修改白皮书 |
| §6 报告样式 | PASS | 未修改样式定义 |

---

## 六、新增模块质量评估

### 6.1 write_alert.ps1（告警模块）

- 路径使用 `$PSScriptRoot`：PASS
- 文件行数31：远低于500限制
- 依赖：仅依赖 `issues.csv` 文件路径
- 幂等性：使用 `-Append` 追加，重复调用不会丢失数据

### 6.2 generate_quality_dashboard.ps1（质量仪表盘）

- 路径使用 `$PSScriptRoot`：PASS
- UTF8NoBOM写入：PASS（Line 88 使用 `UTF8Encoding::new($false)`）
- 文件行数89：低于500限制

### 6.3 project_config.json + config_loader.ps1（配置中心）

- 未在本次审查中直接审查（配置文件不涉及执行逻辑）
- 建议：后续审计中纳入配置一致性检查

---

## 七、发现的问题汇总

| 编号 | 级别 | 层级 | 描述 | 状态 |
|:----:|:----:|:----:|:-----|:----:|
| B1-01 | B1 | L4 | run_keystock_analysis.ps1 = 1985行，超500行限制（预存，非本次引入） | 待 Phase 2 拆分 |
| B2-01 | B2 | L3 | 3个文件使用 `[System.Text.Encoding]::UTF8` 产生BOM | 待后续修复 |
| B2-02 | B2 | L3 | predictions.csv 带UTF-8 BOM | 待后续修复 |

---

## 八、闸门2判定

```
                闸门2: 代码闸
    ┌──────────────────────────────────────┐
    │  L1 变更影响分析 .......... PASS      │
    │  L2 测试验证 .............. QUALIFIED │
    │  L3 回归检查 .............. PASS      │
    │  L4 红线审查 .............. CONDITIONAL│
    │                                      │
    │  B0 阻断: 0                          │
    │  B1 遗留: 1（预存，非本次引入）          │
    │  B2 跟进: 2                          │
    │                                      │
    │  >> 闸门2: PASS <<                   │
    │  条件: B1-01 纳入 Phase 2 拆分计划     │
    └──────────────────────────────────────┘
```

**闸门2判定**：**PASS** — 允许流入下一环节（闸门3，红枫灰度部署准备）。

**放行条件**：
1. B1-01（run_keystock_analysis.ps1超500行）为预存遗留，不阻断本次发布，但须在 Phase 2 轻量化中处理
2. B2-01/B2-02 为低优先级建议，不阻塞

**下一环节**：闸门3 — 红枫灰度部署方案 + 回滚方案就绪确认
