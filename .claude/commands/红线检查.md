description: 对照规则红线逐项审查当前工作合规性

【红线检查】

## 当前文件版本快照

!`Get-ChildItem "C:\Users\34269\Documents\Claude\股票分析" -Recurse -Filter "*_v*.*" | Where-Object Name -match "_v\d+\.\d+" | ForEach-Object { $d = $_.DirectoryName.Split("\"); $d[-2] + "/" + $d[-1] + "/" + $_.Name } | Sort-Object`

## 临时先跑合规检查

运行 check_redlines.ps1 看当前项目状态。

## 检查维度

读取完整红线规则（`规则红线/分析的规则红线--Claude_v1.4.md`）。逐项检查，每条给出具体证据：

### §1 数据真实性
数据来源标记[编号] / PE(TTM)用本地计算 / 双源交叉验证 / 无编造数据

### §2 资源节约
代码可完成的工作是否交给AI做 / 是否全文阅读而非局部读取 / 是否有重复调用

### §3 API缓存
数据分层缓存 / 防封禁间隔0.3s/2s / 降级路径可用

### §4 执行检查清单
来源标记→双源验证→PE(TTM)合规→API延迟→季度缓存→免责声明→一票否决→降级标注→数据时效→未实测标注→不可获取禁输出→PE百分位标注

### §5.4 版本管理
4份白皮书每份检查：独立文件存档 / 旧版保留 / CHANGELOG同步 / DOCX同步 / 版本号格式 `v<主>.<次>`

## 报告格式

```
## ✅ 合规项
...

## ⚠️ 警告项 (标注§编号)
...

## ❌ 违规项 (标注§编号)
...

**总体判定：合规 / 基本合规（有建议）/ 不合规（需修正）**
```
