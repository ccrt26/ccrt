description: 生成ALL重点股票每日简报——baseline→delta→trigger三层，10段，每只独立PDF

# 重点分析日报 — 每日操作简报

## 前置约束
修改前先查 `.claude/knowledge/角色边界宪章.md` §六。无权文件 → 退回阿黑。
> ⛔ 15:37自动触发。以深度为baseline，做delta+trigger，服务"明日操作"。
> **方法论**: `重点股票/分析逻辑/重点股票跟踪分析逻辑白皮书_v3.6.3.md`（v3.6.3 + D07_v1.2 full_release）
> **详细流程**: `.claude/knowledge/日报-详细流程.md`

## 核心铁律
1. 不做深度重做。只做 baseline→delta→trigger
2. 用户版MD禁止 machine-only、数据源编号、数据完整度工程表、信号快照
3. 机器字段在同名 JSON sidecar
4. P0决策卡九字段缺一不可
5. 必须通过 U检查+PDF gate 后生成PDF
6. ⛔ **统一解读强制**：每只生成 interpretation_schema.json → U-9/U-10 → eval_hooks → sidecar；audit_u9/audit_u10 必须来自真实U检查

### 发布前硬规则（2026-06-03）
7. `publish_allowed==false` 或 `skip_data_check==true` → 禁止输出
8. 成交量只能用 `days[].volume_wan_shou`
9. sidecar `fund_flow_4level` 必须完整写入五项
10. sidecar 必须写入 `sector_phase.phase`
11. 发布前跑 `check_daily_release_gate.py --date YYYYMMDD`

### D07_v1.2 全量发布铁律（2026-06-13）
12. ✅ 已全量启用，不再灰度。新日报默认使用 D07_v1.2 结构
13. 社保/养老金仅触发式启用（财报季/持仓记录/逻辑争议/周报链/用户要求）
14. 问题不回灰度，直接走 F-FIX 定点修
15. canonical E3 cutover 与 D07_v1.2 全量口径独立

## 产出与闸门
- 正文、数据源、样式、全域解读详见 `.claude/knowledge/日报-详细流程.md`
- 发布前通过：gate + style + collaborative 三检查
- MD→HTML→PDF，`generate_portal.py` → `git push`

## 重点股票池
> ⛔ **权威源**：`代码文件/信鸽信息采集/pigeon_config.json`
