# 当前环境配置快照 — 红枫部署工程师

> 版本 v1.0 | 生效日期 2026-05-24
> 用途：生产环境的精确配置记录——版本、路径、依赖清单。任何变更后24小时内更新。
> 红枫负责维护，每次部署前对照此快照做兼容性检查。

---

## 一、系统环境

| 项 | 值 | 备注 |
|:---|:----|:-----|
| 操作系统 | Windows 11 Pro China Only 10.0.26200 | |
| 用户 | 34269 | |
| 项目根目录 | `C:\Users\34269\Documents\Claude\股票分析\` | |

---

## 二、Python 环境

| 项 | 值 |
|:---|:----|
| Python 版本 | 3.13.13 |
| 可执行路径 | `C:\Users\34269\AppData\Local\Programs\Python\Python313\python.exe` |
| 虚拟环境 | 未使用(全局Python) |
| pip 版本 | (见下方包清单) |

### 已安装关键包

| 包名 | 版本 | 用途 |
|:-----|:----:|:-----|
| akshare | 1.18.63 | A股数据采集(核心依赖) |
| numpy | 2.4.6 | 数值计算 |
| openpyxl | 3.1.5 | Excel读写 |
| requests | (httpx 0.28.1) | HTTP请求 |
| lxml | 6.1.1 | XML/HTML解析 |
| beautifulsoup4 | 4.14.3 | HTML解析 |
| curl_cffi | 0.15.0 | 反反爬HTTP请求 |
| fpdf2 | 2.8.7 | PDF生成 |
| mcp | 1.27.1 | MCP服务器 |
| jsonschema | 4.26.0 | JSON Schema验证 |
| cryptography | 48.0.0 | 加密支持 |
| fonttools | 4.63.0 | 字体处理 |
| cssselect2 | 0.9.0 | CSS选择器 |
| markdown-it-py | 4.2.0 | Markdown解析 |

> 完整清单更新日期：2026-05-24

---

## 三、PowerShell 环境

| 项 | 值 |
|:---|:----|
| PowerShell 版本 | 5.1.26100.8457 |
| 执行策略 | Restricted ⚠ (注意：需管理员权限改为 RemoteSigned 才能运行项目.ps1脚本) |
| 项目脚本目录 | `代码文件/` |

---

## 四、Git 环境

| 项 | 值 |
|:---|:----|
| Git 用户 | 铁律量化 |
| 当前分支 | master |
| 远程仓库 | 待确认(`git remote -v`) |

---

## 五、定时任务注册状态

| 任务名 | 状态 | 上次运行 | 下次运行 |
|:-------|:----:|:--------|:--------|
| — | — | — | — |

> 当前无注册的铁律量化定时任务。每日流程首次运行前需注册。

---

## 六、目录结构(关键路径)

| 用途 | 路径 |
|:-----|:-----|
| 白皮书/规则红线 | `规则红线/` |
| 代码 | `代码文件/` |
| 审计报告 | `审计报告/` |
| 临时报告 | `临时报告/` |
| 历史数据 | `历史数据/` |
| 每日荐股 | `每日荐股/` |
| 重点股票 | `重点股票/` |
| 共享知识库 | `.claude/knowledge/` |
| 角色定义 | `.claude/agents/` |
| 角色命令 | `.claude/commands/` |

---

## 七、环境健康检查清单

每次部署前运行以下检查：

```
[✓] Python 版本兼容(≥3.9 → 3.13.13 ✓)
[✓] 关键包已安装(akshare/numpy/openpyxl ✓)
[⚠] PowerShell 执行策略为 Restricted — ps1脚本需要在powershell.exe -File调用中运行，或改为RemoteSigned
[ ] Git 仓库无未提交更改(或已登记)
[ ] 网络可访问 [1]腾讯 [2]新浪 [3]东财 API
[ ] 缓存目录存在且可写
[ ] 磁盘空间 ≥ 1GB
[ ] Task Scheduler 服务运行中
```

---

## 八、维护规则

- 首次部署时完整填充本表 ✅ (2026-05-24)
- 每次环境变更(升级/安装/卸载)后24小时内更新
- 每季度全量重新采集环境信息
- 环境变更前先在非生产路径测试
