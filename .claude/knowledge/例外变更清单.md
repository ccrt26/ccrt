# 例外变更清单

> 记录不属于 fix4 三件事（测试隔离/范围清理/阿黑越权验收）范围的例外变更申请。
> 每条记录需注明：变更项、判定依据、处理方式、日期、处理人。

---

## 记录

### 001 — pipeline_auth.py 归属恢复

| 字段 | 内容 |
|:-----|:-----|
| **日期** | 2026-05-31 |
| **处理人** | 千光 |
| **变更文件** | `.claude/hooks/shared/pipeline_auth.py`（下划线版本） |
| **变更性质** | 文件恢复（从 /tmp/pipeline_auth.py.moved 移回工作区原位置） |
| **fix4 判定** | **不属于 fix4 范围** |
| **判定依据** | pipeline_auth.py 是 write protection auth module（L2 安全基础设施），用于 pre-commit-check.py 和 write_protection_hook.py 的共享鉴权。该文件属于 CI/CD 流水线基础设施，与阿黑权限边界相关（写保护鉴权模块），但不属于 fix4 三件事：1) 非测试隔离 2) 非范围清理 3) 非阿黑越权验收 |
| **分类** | 例外变更申请 |
| **处理方式** | 文件已恢复至 `.claude/hooks/shared/pipeline_auth.py`（与旧连字符版本 `pipeline-auth.py` 并存）。下划线版本较连字符版本新增 6 条 AutoCommit 豁免路径（im_queue/knowledge/pipeline_history/regen/sweep.lock/deploy_requested）。 |
| **状态** | 已恢复（untracked，待后续 git add） |

---

## 模板

```markdown
### NNN — <简述>

| 字段 | 内容 |
|:-----|:-----|
| **日期** | YYYY-MM-DD |
| **处理人** | <角色名> |
| **变更文件** | <路径> |
| **变更性质** | <新增/修改/删除/恢复> |
| **fix4 判定** | 属于/不属于 fix4 范围 |
| **判定依据** | <详细说明> |
| **分类** | 例外变更申请 |
| **处理方式** | <处理描述> |
| **状态** | <待处理/已完成> |
```
