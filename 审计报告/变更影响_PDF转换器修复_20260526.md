# 变更影响分析 — PDF转换器修复

> **日期**：2026-05-26 | **变更**：convert_daily_brief_pdf.py md_to_html() 重写

## 变更内容
- `md_to_html()` 从5步正则替换改为状态机单次遍历
- 新增 `_inline_format()` / `_is_table_row()` / `_is_alignment_row()` 辅助函数
- Edge 命令增加 `--no-pdf-header-footer`
- CSS 新增 `p` / `ul` / `li` 样式

## 影响范围
| 维度 | 评估 |
|:-----|:-----|
| 上游 | gen_daily_brief.py 不变 |
| 下游 | 7份PDF重新生成，MD源不变 |
| 数据 | 不涉及评分/排序/交易逻辑 |
| 回滚 | 还原文件即可，无数据迁移 |

## 风险评估
低风险。仅影响MD→HTML展示层，不影响任何数据管线。
