# 回滚说明 — 四合一工程修复 (RUN-20260531-161857-348b53)

> 日期: 2026-06-01 | run_id: RUN-20260531-161857-348b53

## 回滚触发条件

任一验收测试在回滚后环境复现失败，或变更引入新的回归问题。

## 逐项回滚操作

### F1: 治理路径写保护 (pipeline_auth.py)

```bash
git checkout HEAD~1 -- .claude/hooks/shared/pipeline_auth.py
```
回滚后状态: GOVERNANCE_PATHS 列表移除，AUTOCOMMIT 豁免恢复无条件优先。

### F2: 飞书真实配置 (~/.feishu_bot_tmp.json)

```bash
echo '{"app_id":"cli_aa939df4f6385cbb","app_secret":"7GTJaACKciyBQLhWmDaESZv3c85ibQzF","chat_id":"oc_d79295e32b9f10312315b11bf1701be8"}' > ~/.feishu_bot_tmp.json
chmod 600 ~/.feishu_bot_tmp.json
```
注意：回滚后 feishu_bridge.py 将因缺 allowed_users/command_whitelist/route_table 而拒绝启动。

### F3: 飞书测试配置 (test_feishu_command_guard.py)

```bash
git checkout HEAD~1 -- 代码文件/监督机制/test_feishu_command_guard.py
```

### F4: git_autocommit dry-run (git_autocommit.py)

```bash
git checkout HEAD~1 -- 代码文件/tools/git_autocommit.py
```
回滚后状态: dry_run=True 时 git add 仍会执行，暂存区会被污染。

### F5: 定时任务 (scheduled_tasks.json)

```bash
git checkout HEAD~1 -- .claude/scheduled_tasks.json
```
回滚后状态: sim_trading_daily 任务使用错误路径和 -Force 参数，调度将失败。

## 全量回滚

```bash
# 回滚所有代码变更
git checkout HEAD~1 -- .claude/hooks/shared/pipeline_auth.py
git checkout HEAD~1 -- 代码文件/tools/git_autocommit.py
git checkout HEAD~1 -- 代码文件/监督机制/test_feishu_command_guard.py
git checkout HEAD~1 -- .claude/scheduled_tasks.json

# 飞书配置回滚
echo '{"app_id":"cli_aa939df4f6385cbb","app_secret":"7GTJaACKciyBQLhWmDaESZv3c85ibQzF","chat_id":"oc_d79295e32b9f10312315b11bf1701be8"}' > ~/.feishu_bot_tmp.json
chmod 600 ~/.feishu_bot_tmp.json
```

## 回滚后验证

```bash
python3 代码文件/监督机制/test_protection_chain.py
python3 代码文件/监督机制/test_feishu_command_guard.py
python3 代码文件/tools/git_autocommit.py --module engineering --paths 代码文件/tools/health_check.py --message test --dry-run
```

## 回滚风险

| 风险 | 级别 | 说明 |
|:-----|:----|:-----|
| 飞书桥接不可用 | P1 | F2回滚后 feishu_bridge 因缺安全配置拒绝启动 |
| 定时任务失败 | P1 | F5回滚后 sim_trading_daily 因路径不存在而失败 |
| 写保护绕过 | P2 | F1回滚后 .claude 治理文件可被扩展名豁免 |
| 暂存区污染 | P2 | F4回滚后 dry-run 会执行 git add |
