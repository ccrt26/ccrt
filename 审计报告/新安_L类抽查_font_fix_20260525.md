# 新安 — L类变更抽查

> **变更对象**: convert_md_to_pdf.py CSS font-family 修复
> **审查者**: 新安 | **日期**: 2026-05-25
> **变更等级**: L类（单文件，~3行，不改接口）

---

## 一、变更内容

| 文件 | 变更 | 行数 |
|:-----|:-----|:----:|
| `代码文件/tools/convert_md_to_pdf.py` | CSS 新增 `pre` 规则 + `code` 规则补 `font-family` | +3行 |

**变更前**：`code` 选择器未设 `font-family`，`pre` 元素无样式 → fenced code block 渲染为浏览器默认等宽字体 (Consolas)，与正文"微软雅黑"不一致。

**变更后**：`pre` 和 `code` 均显式指定 `font-family: "Microsoft YaHei", "微软雅黑", "SimHei", sans-serif`，与 body 字体一致。

## 二、L类判定复核

| 条件 | 结果 |
|:-----|:----:|
| ① 单文件 | PASS |
| ② ≤50行 | PASS（+3行CSS） |
| ③ 不改变函数签名/接口/数据格式 | PASS（纯CSS修改） |
| ④ 不涉及评分/交易/风控逻辑 | PASS（纯展示层） |
| ⑤ 不涉及L2模块 | PASS（L0工具） |

## 三、验证结果

| 检查项 | 结果 |
|:-------|:----:|
| Python语法正确 | PASS |
| 批量生成 8/8 PDF | PASS |
| PDF大小正常 (370-603 KB) | PASS |
| 代码块字体与正文一致 | PASS（CSS已显式设置） |

## 四、抽查结论

**L类变更抽查 PASS**。变更最小化、目标明确、无副作用。CSS 修复正确，8 份 PDF 重新生成全部成功。
