# 红枫 — 回滚方案

> **回滚对象**: MD→PDF 工具链
> **制定者**: 红枫 | **日期**: 2026-05-25

---

## 一、回滚触发条件

| 条件 | 严重度 |
|:-----|:----:|
| convert_md_to_pdf.py 语法错误导致无法导入 | P2 |
| Edge headless 参数变更导致PDF生成失败 | P2 |
| PDF输出质量不达标（乱码/格式严重错位） | P2 |
| 日期预检误报/漏报导致不一致报告 | P2 |

## 二、回滚操作

### 方案A: 文件级回滚（推荐）

```powershell
# 删除两个新增文件
Remove-Item "代码文件/tools/convert_md_to_pdf.py"
Remove-Item "代码文件/tools/batch_gen_keystock_pdfs.py"
```

### 方案B: Git回滚

```bash
git checkout -- 代码文件/tools/convert_md_to_pdf.py
git checkout -- 代码文件/tools/batch_gen_keystock_pdfs.py
```

## 三、回滚验证

| 验证项 | 方法 |
|:-------|:-----|
| 文件已删除 | `ls 代码文件/tools/convert_md_to_pdf.py` 应不存在 |
| 现有工具不受影响 | `gen_keystock_pdf.ps1` 正常运行 |
| 报告生成不受影响 | 可继续手动生成PDF |

## 四、回滚影响

- **影响范围**: 无。两文件为纯新增，无其他模块依赖它们
- **用户影响**: 无。无自动调度绑定
- **数据影响**: 无。中间HTML/PDF可手动清理

## 五、不可回滚条件

无。L0工具零耦合，随时可安全删除。
