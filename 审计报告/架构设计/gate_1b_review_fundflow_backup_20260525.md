# 闸门1b 技术合规审查 — 资金流四源备份架构

> 审查人：新安（质量）+ 旧影（审计）
> 审查对象：design_fundflow_multi_backup_v1.0.md
> 日期：2026-05-25
> gate: PASS

---

## 一、新安 — 质量审查

### 1.1 设计完整性

| 检查项 | 结果 | 说明 |
|:-------|:----:|:-----|
| 接口契约定义 | ✅ | 函数签名不变，返回值格式不变，下游零适配 |
| 错误处理规范 | ✅ | 每备源独立try/catch，失败不阻断后续 |
| 字段映射机制 | ✅ | 桥接层内部映射，输出统一格式。智兔/StockTV标注"待文档确认" |
| 缓存策略 | ✅ | 复用现有24h TTL + 720h兜底，无变更 |
| 回退方案 | ✅ | git revert单次提交即可 |
| 模块归类 | ✅ | 桥接脚本遵循THS模式，L1标注正确 |
| 引擎变更 | ✅ | **不修改**Invoke-DataSource引擎，BackupCall内链实现 |

### 1.2 影响范围验证

| 下游模块 | 影响路径 | 评估 |
|:---------|:---------|:----:|
| scoring_engine_v2.py | 读缓存JSON | ✅ 无影响 |
| batch_data_collector.ps1 | 调用Get-StockFundFlow | ✅ 签名不变 |
| build_dynamic_pool.ps1 | 读data_full.json | ✅ 无影响 |
| gen_daily_html.ps1 | 读评分JSON | ✅ 无影响 |
| sim_trading.ps1 | 调用Get-StockFundFlow | ✅ 签名不变 |
| gen_doc_v2.ps1 | 读评分JSON | ✅ 无影响 |

### 1.3 测试策略建议

| 测试级别 | 内容 | 验证方法 |
|:--------:|:-----|:---------|
| 单元 | 每个桥接脚本独立可运行 | `python stock_data_fetcher_diya.py fund_flow --code 000001 --days 5` |
| 集成 | 降级链完整遍历 | 模拟东方财富503 → 验证迪雅→麦蕊→智兔→StockTV逐级尝试 |
| 回归 | 评分引擎输出不变 | 对比接入前后data_scored.json的TotalScore |
| 红线 | 1+2→1+4架构 | 检查SourceUsed日志，确认降级链实际执行 |

### 1.4 代码等级确认

| 变更 | 等级 | 审查要求 |
|:-----|:----:|:---------|
| fundflow.ps1 BackupCall + 4包装函数 | L1 | Golden Master diff |
| 4个Python桥接脚本(新增) | L1 | 独立运行测试 + 字段对齐验证 |
| core.ps1 SourceRegistry更新 | L1 | 配置一致性检查 |

**新安结论：通过。设计完整，影响范围可控，测试策略明确。智兔/StockTV字段映射的不确定性已在设计中标注，编码阶段消解即可。**

---

## 二、旧影 — 审计审查

### 2.1 红线合规检查

| 红线条款 | 检查 | 结果 |
|:---------|:-----|:----:|
| §1.1 数据源1+2主备架构 | 升级为1+4+1，远超最低要求 | ✅ |
| §1.2 PE(TTM)公式 | 不涉及评分公式变更 | ✅ |
| §1.3 禁止编造数据 | 桥接仅透传API原始数据 | ✅ |
| §1.7 禁止删除PDF | 无PDF操作 | ✅ |
| §5.4 文档同步 | 设计文档无对应.docx需求 | ✅ N/A |
| §9.1 代码分级 | L1正确标注 | ✅ |
| §9.2 单文件≤500行 | fundflow.ps1 283→~403行 <500 | ✅ |

### 2.2 反模式检查

| 反模式 | 触发？ | 说明 |
|:-------|:------:|:-----|
| AP-01 循环依赖 | 否 | core←fundflow单向, 桥接脚本无循环 |
| AP-02 上帝模块 | 否 | 每桥接脚本职责单一(一个API) |
| AP-03 硬编码路径 | 否 | API密钥用环境变量, 脚本路径用$PSScriptRoot |
| AP-04 配置散落 | 否 | SourceRegistry集中管理, 沿用框架模式 |
| AP-05 静默失败 | 否 | 每源失败有Write-Warning, SourceUsed记录最终源 |
| AP-06 代码重复 | **注意** | 4个桥接脚本结构相似, 但每源有独立字段映射逻辑, 分离比合并更利于故障隔离。此为有意的设计选择, 非反模式。 |

### 2.3 流程合规

| 检查 | 结果 |
|:-----|:----:|
| 情墨阶段①完成(pipeline_stage: complete) | ✅ |
| 腰子闸门1a确认(gate_1a: PASS, 全团咨询通过) | ✅ |
| 设计文档含需求→代码核对清单(12项) | ✅ |
| 版本号 | v1.0 合理 |
| CHANGELOG | N/A (无代码变更) |

### 2.4 变更分类验证

| 判定条件 | 结论 |
|:---------|:-----|
| 涉及代码文件/ | 是 → E类工程变更 |
| 新增功能 | 是 → 4个新桥接脚本 |
| 影响>3文件 | 是 → 6个文件 |
| 数据格式变更 | 否 → 函数签名不变, 返回值不变 |

> 分类：E类 - 走完整§七流程 ✅ 当前正在执行

**旧影结论：通过。AP-06相似代码属有意设计选择(故障隔离)，非反模式。红线合规，流程合规。**

---

## 三、联合判定

| 审查维度 | 新安 | 旧影 |
|:---------|:----:|:----:|
| 设计完整性 | ✅ | — |
| 影响范围可控 | ✅ | — |
| 测试策略明确 | ✅ | — |
| 红线合规 | — | ✅ |
| 反模式检查 | — | ✅ |
| 流程合规 | — | ✅ |
| 代码等级 | ✅ | ✅ |

**Gate 1b: PASS** ✅

闸门1通过。放行至红结编码实现。

---

> 审查完成 | 2026-05-25 | gate: PASS
