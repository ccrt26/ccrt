# 变更记录 — 四合一工程修复 (RUN-20260531-161857-348b53)

> 日期: 2026-06-01 | run_id: RUN-20260531-161857-348b53

## F1: .claude/hooks/shared/pipeline_auth.py (L2)

**变更前**: AUTOCOMMIT_PATHS/AUTOCOMMIT_EXTENSIONS 在 Step 0 无条件豁免所有匹配路径，.claude/hooks/*.py 可因 .py 扩展名绕过 pipeline token 校验。

**变更后**:
- 新增 `GOVERNANCE_PATHS` 列表（7个治理路径正则）
- `test_pipeline_authorization()` 中 Step 0 改为治理路径判定 → 命中则跳过 AUTOCOMMIT 豁免
- 治理路径并入保护路径联合检查（Step 2）
- 步骤重新编号（Step 0-5 → Step 0-5）

**diff**: 新增 ~10 行 GOVERNANCE_PATHS 定义，函数内逻辑重构 ~15 行。

## F2: ~/.feishu_bot_tmp.json (L0)

**变更前**: 仅含 app_id, app_secret, chat_id 三项。

**变更后**: 补齐 allowed_users, command_whitelist, route_table 三项。权限保持 600。
- allowed_users: ["ou_placeholder"]
- command_whitelist: ["/日报", "/深度分析", "/保护", "/状态"]
- route_table: {"/日报": "daily", "/深度分析": "deep", "/保护": "shield", "/状态": "status"}

## F3: 代码文件/监督机制/test_feishu_command_guard.py (L1)

**变更前**: 临时测试配置缺 route_table（与 feishu_bridge.py 的 _validate_config() 新增检查不一致）。无缺配置拒绝启动测试。

**变更后**:
- T2 临时配置补齐 route_table: {"/日报": "daily", "/保护": "shield"}
- 新增 T4 测试段：T4.1（缺 route_table → 拒绝）、T4.2（缺 allowed_users → 拒绝）、T4.3（缺 command_whitelist → 拒绝）
- 使用 SystemExit 捕获验证拒绝行为

**diff**: +3 行配置修改，+40 行 T4 测试段。

## F4: 代码文件/tools/git_autocommit.py (L1)

**变更前**: `git_autocommit()` 在 dry_run=True 时仍执行 `git add`（第69行），仅在 commit 前 return。暂存区被污染。

**变更后**:
- dry_run 路径完全隔离：不执行 git add / git commit / git push
- 仅计算：路径存在性（含 git ls-files 回退）、would_stage 列表、files_count
- result 字典含 `"dry_run": true` 标记，不含 `"commit_hash"` 键
- commit 命令注释标记 "never --no-verify"

**diff**: 函数重写 ~60 行，逻辑流重构。

## F5: .claude/scheduled_tasks.json (L0)

**变更前** (sim_trading_daily 任务):
```
python3 "模拟交易/交易引擎/sim_orchestrator.py" -Force
python3 "代码文件/模拟交易/generate_dashboard.py"
```

**变更后**:
```
python3 "模拟交易/sim_orchestrator.py" --force --dry-run
python3 "模拟交易/分析/generate_unified_view.py"
```

三项修正：错误子目录(交易引擎)、PowerShell -Force → Python --force、不存在脚本 → 真实脚本。
