# 新安四层验证报告 — 流程引擎化 v1.0

> 验证人: 新安 | 日期: 2026-05-25 | 代码等级: L1
> 变更: pipeline_engine.ps1(新) + pipeline_token.ps1(改) | 设计: design_pipeline_engine_v1.0_流程引擎化.md

---

## 第一层：变更影响分析

### 变更清单

| 文件 | 类型 | 行数 | 影响范围 |
|:-----|:----:|:----:|:--------|
| pipeline_engine.ps1 | 新增 | ~300 | 流程状态管理核心 |
| pipeline_token.ps1 | 修改 | ~137 (原213) | CLI wrapper，内部委托给engine |

### 影响矩阵

| 受影响的系统组件 | 影响方式 | 风险等级 | 缓解措施 |
|:--------------|:--------|:------:|:--------|
| write_protection_hook.ps1 | 读 `pipeline_active.json` 的 `active`+`executor` | **无影响** | 字段保留，schema向后兼容 |
| pre-commit-check.ps1 | 同上 | **无影响** | 同上 |
| pipeline_history/ | engine写历史JSON，格式兼容 | **低** | 新增字段不破坏旧读取 |
| 阿黑流程驱动 | 从读CLAUDE.md改为读engine JSON | **中** | 需同步更新CLAUDE.md §七 |
| 旧 pipeline_token.ps1 调用者 | CLI接口不变 | **无影响** | -Start/-Advance/-Complete/-Status全兼容 |

### 新增依赖

- pipeline_token.ps1 → pipeline_engine.ps1（同目录，`$PSScriptRoot` 相对路径）
- pipeline_engine.ps1 → git（仅 -Validate stage 3 时需要）

---

## 第二层：测试报告

### 功能测试

| 测试项 | 操作 | 预期 | 实测 | 判定 |
|:------|:-----|:-----|:-----|:----:|
| -Status (无活跃流程) | 新环境调用 -Status | JSON `active:false` | `active:false, stage:0` | ✅ PASS |
| -Status (有活跃流程) | 调用 -Status | JSON含stage/executor/next_action | stage=4, executor=新安, next_action=invoke_role | ✅ PASS |
| Schema 迁移 | 旧格式 token → -Status | 自动补充 schema_version 等字段 | `[Engine] Schema migrated to v1.0` | ✅ PASS |
| -Start | 新任务启动 | stage=1, executor=情墨 | Token created correctly | ✅ PASS |
| -Advance | 阶段推进 | stage+1, executor更新 | Advanced to Stage 4/6 | ✅ PASS |
| -Advance -Force | 跳过闸门推进 | 警告+推进 | WARNING: Gate bypassed + Advanced | ✅ PASS |
| -Complete | 归档+重置 | active=false, 归档JSON | Pipeline completed and archived | 待验证* |
| -Validate (stage 1) | 检测设计文档 | 检测 markdown 标记 | 未在本轮测试（需设计文档含标记） | ⏸️ 跳过 |
| -Retry | 重试计数 | attempts+1 | 未触发（attempts=0） | ⏸️ 跳过 |

\* -Complete 未测试，当前流程进行中不宜测试（会重置活跃流程）

### 已知问题

| 编号 | 级别 | 描述 | 状态 |
|:----|:----:|:-----|:----:|
| **T1** | **P2** | Test-Stage3Complete: git UTF-8输出在PowerShell 5.1下编码不匹配，中文路径检测失败 | 已定位，补丁已应用（`[Console]::OutputEncoding`），待bash环境验证 |
| T2 | P3 | `$CODE_DIR` 变量定义但未使用 | IDE warning，不影响功能 |
| T3 | P3 | pipeline_token.ps1 中文注释乱码 | BOM写入时编码损坏，需重写 |

---

## 第三层：回归验证

### Hook 兼容性验证

**write_protection_hook.ps1** (代码文件写入保护):
```powershell
$token.active -eq $true          # ✅ 字段保留，值类型不变(boolean)
$token.executor -eq "红结"       # ✅ 字段保留，值类型不变(string)
```
判定: **PASS** — 无需修改，向后兼容。

**pre-commit-check.ps1** (提交前门禁):
```powershell
$token.active -eq $true          # ✅ 字段保留
$token.executor -eq "红结"       # ✅ 字段保留  
$token.stage                     # ✅ 字段保留(int)
```
判定: **PASS** — 无需修改，向后兼容。

### 流程历史兼容性

- 旧 pipeline_history/*.json 文件: engine 不读取旧历史，仅追加新记录。✅
- pipeline_active.json 旧格式: engine 自动迁移，补充 `schema_version` 等字段。✅

### Golden Master 说明

本次变更不涉及评分/排序/否决/相位逻辑（L2 范围），Golden Master diff 不适用。替代验证：引擎 -Status 输出的 next_action 与设计文档 §五 数据流图一致。

---

## 第四层：红线审查

对照规则红线 v1.16 逐项检查：

| 条款 | 检查内容 | 判定 |
|:-----|:--------|:----:|
| §1.1 数据源1+2 | 不涉及数据采集 | N/A |
| §1.2 关键公式 | 不涉及PE/TTM计算 | N/A |
| §1.3 禁止编造 | 引擎不产出金融数据 | ✅ |
| §1.7 禁止删PDF | 不涉及PDF操作 | ✅ |
| §2 文档同步 | .md → .docx 同步（设计文档已建议W1） | ⚠️ 见注1 |
| §4 执行前自查 | 本报告即为执行自查 | ✅ |
| §5.4 CHANGELOG | code commit 时记录 | 待commit |
| §9.1 代码分级 | L1 标注已确认 | ✅ |
| §9.2 单文件≤500行 | engine ~300行，token ~137行 | ✅ |
| §9.2 自查清单 | 红结提交前自查（§八清单） | 待commit时执行 |

**注1**: 设计文档 .docx 同步 — 旧影 W1 已标注，流程完成后批量生成。不阻塞闸门通过。

### 安全审查

| 检查项 | 状态 |
|:------|:----:|
| Invoke-Expression 注入风险 (pipeline_token.ps1:30) | ⚠️ 低风险 — `$EngineArgs` 来自脚本内部硬编码字符串，非用户输入 |
| 路径遍历 | ✅ — 所有路径基于 `$PSScriptRoot` 解析 |
| 文件权限 | ✅ — 写入前检查目录存在性 |
| JSON 解析异常 | ✅ — ConvertFrom-Json 有 try/catch 保护 |

---

## 闸门2 判定

| 层级 | 判定 | 阻塞项 |
|:-----|:----:|:------|
| 变更影响分析 | ✅ PASS | 无 |
| 测试报告 | ⚠️ PASS(条件) | T1/T3 非阻塞 |
| 回归验证 | ✅ PASS | 无 |
| 红线审查 | ✅ PASS | 无 |

> **综合判定: PASS** — 闸门2通过。T1(编码)/T3(乱码)为平台环境问题，不阻塞推进，建议在红枫部署前修复。

> 新安签字: ✅ 四层验证完成
