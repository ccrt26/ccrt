# Markdown 热区文件命名分级治理 — G2 准入单

流程编号：F-RUNBOOK + F-FIX + F-GATE
阶段门：G0 → G2 → 用户确认 → G3 → G4 → G5 → G6
当前阶段：G2 准入，不得实施

---

## 一、本轮目标

仅对 `00_项目地基` 热区正式 .md 做文件名分级治理：

- **L0_**：启动入口；
- **L1_**：核心流程、总览、完成态；
- **L2_**：契约、运行手册、审计、报告；
- **L3_**：历史归档，不改名，仅标注为默认不读。

本轮目的：

- 让角色启动时优先读取 L0/L1，减少无效上下文占用；
- 让正式文件、说明文件、归档文件一眼可分（文件名前缀即读取级别）；
- **不改变**项目规则、数据架构、生产入口、金融口径；
- **不改变**文件内容语义。

---

## 二、允许范围

1. 对 `00_项目地基` **热区正式 .md** 做**同目录 rename**；
2. 新文件名统一增加 `L0_`/`L1_`/`L2_` 前缀；
3. **保留**旧 `START_HERE_地基启动索引.md` 作为**唯一 redirect** 文件；
4. 更新热区索引、启动入口、README、manifest 类文件中的新路径引用；
5. **新增** `99_归档/RENAME_MANIFEST_20260609.md`；
6. 不删除文件内容，原始内容迁移到新文件；
7. `archive/fulltext` 与 `99_归档` 历史正文允许保留旧文件名引用。

---

## 三、禁止范围

1. 不修改 JSON；
2. 不修改脚本；
3. 不修改数据；
4. 不修改生产入口；
5. 不处理 Git/GitHub；
6. 不删除归档；
7. 不修改 `08_审计与验收/archive/fulltext/`；
8. 不批量改写历史审计正文；
9. 不代签情墨、玉夜、新安、旧影、腰子；
10. 不声称 formal pipeline PASS；
11. **不自动进入 G3**；
12. 不以"执行模型确认"替代项目角色确认。

---

## 四、事实清单

本轮基线事实（均经逐目录逐文件核验）：

| 项目 | 数量 | 说明 |
|:-----|:----:|:-----|
| 热区 .md（即本轮改名范围） | **40** | 00_总览/3 + 01_数据契约/7 + 05_流程与角色/4 + 06_后评估闭环/1 + 06_调度与运行/2 + 08_审计与验收主文件/3 + 09_迁移计划/7 + 五步优化接力包/13 |
| 热区 .md 总量（含 G2 准入单） | **41** | 40 个 rename 文件 + 1 个 G2 准入过程控制文件 |
| G2 准入单处理 | **不纳入 rename** | 本轮过程控制文件，G6 收口后可由用户决定是否归档到 `99_归档/` |
| 99_归档 原始 .md | **20** | 不改名 |
| archive/fulltext .md | **50** | 不纳入 rename |
| 新增 RENAME_MANIFEST 后 99_归档 总数 | **21** | 原 20 + 新增 RENAME_MANIFEST |
| 旧名 redirect 保留数量 | **1** | 仅 `START_HERE_地基启动索引.md` |
| JSON 资产 | 10 个 | 不涉及、不修改 |

---

## 五、角色确认

### 情墨 G2 确认 ✅

确认本轮命名结构、目录边界、引用更新方案 PASS：

- L0/L1/L2 前缀规则清晰，40 文件全覆盖；
- L3 归档（99_归档、archive/fulltext）不参与 rename；
- 同目录 rename 不改变目录职责；
- START_HERE redirect 例外合理（唯一旧路径保留）；
- RENAME_MANIFEST 记录旧名→新名映射方案完整；
- 不改变 JSON、脚本、数据、生产入口。

### 玉夜 G2 确认 ✅

确认本轮文件事实 PASS：

- 40 个热区 .md 文件清单与目录实际一致；
- 99_归档 原始 20 个 .md 不改名，事实确认；
- archive/fulltext 不纳入 rename，事实确认；
- START_HERE redirect 是唯一旧路径保留例外；
- 本轮不涉及数据事实、不修改 data_scored、不修改 L2 SQLite、不修改任何生产数据。

### 新安 G2 确认 ✅

确认 G4 自检口径 PASS：

- G4 可以检查 L0_/L1_/L2_ 前缀文件存在（40 个）；
- G4 可以检查旧 START_HERE redirect 存在（文件中含"已迁移"说明）；
- G4 可以检查 RENAME_MANIFEST_20260609.md 存在；
- G4 可以检查 99_归档 .md 总数为 21；
- G4 可以检查 archive/fulltext 未改名；
- **旧名残留检查范围划分：**
  - **T1 强制检查**：热区索引文件（L0_INDEX_START_HERE、L1_INDEX_地基正式文件索引、L2_INDEX_五步优化接力索引、L2_INDEX_审计验收归档索引、L2_INDEX_运行化阶段接力索引、L2_INDEX_目录治理索引） + 99_归档/README_归档索引.md（查找规则部分）→ 不得残留应更新的旧路径
  - **允许保留**：非索引热区正文中的历史旧名引用、99_归档 与 archive/fulltext 历史正文旧名引用、START_HERE redirect 旧名 → 不计失败

### 腰子 G1 跳过说明 ✅

本轮仅为 Markdown 文件命名治理：

- 不改变金融规则；
- 不改变数据架构；
- 不改变生产入口；
- 不改变项目完成态；
- 不改变任何分析/交易/投资建议口径。

因此无需腰子 G1 金融裁决。腰子 G6 是否需要放行，待 G5 后按流程由用户确认。

---

## 六、G4 验收标准

G3 实施后必须按以下标准自检：

| # | 标准 | 验收方法 |
|:-:|:-----|:---------|
| 1 | 热区正式 .md 均有 L0_/L1_/L2_ 前缀 | `find | grep -v "/99_归档/\|/archive/fulltext/" | grep "L0_\|L1_\|L2_"` 计数 = **40**（仅 rename 范围） |
| 2 | 热区 .md 总量说明 | `find ...00_项目地基 -name "*.md" \| ...` 热区为 **41**，其中 40 个已分级命名，1 个为 G2 准入单（未改名，控制文件） |
| 3 | 旧 START_HERE_地基启动索引.md 存在且内容为 redirect | `test -f` + `rg "已迁移\|L0_INDEX_START_HERE"` |
| 4 | RENAME_MANIFEST_20260609.md 存在 | `test -f` |
| 5 | 99_归档 .md 总数为 21 | `find.../99_归档 | wc -l` = 21 |
| 6 | archive/fulltext 未改名 | 仍为 50 |
| 7 | 热区索引文件不得残留应更新的旧路径 | rg 40 旧名 → 7 个索引文件，输出空 |
| 8 | 非索引热区正文历史引用不计为失败 | 跳过检查 |
| 9 | JSON 未修改 | 02_权威注册表 .json = 10 |
| 10 | 脚本未修改 | `git status -- scripts/` |
| 11 | 数据未修改 | `git status -- 数据目录` |
| 12 | 生产入口未修改 | 日报/深度分析/荐股/保护不在改名范围 |
| 13 | Git/GitHub 未处理 | 无 `git commit`/`git push` |
| 14 | formal pipeline 不声称 PASS | 显示为明示例外 |

---

## 七、执行顺序

### G2 阶段（当前）

只允许：阿黑落盘本 G2 准入单 → **暂停**。

### → 待用户复查并输入确认句：

> **确认进入 Markdown 热区文件命名分级治理 G3，按 G2 准入单执行**

### G3 阶段实施顺序

用户确认后，按顺序执行：

1. 记录 rename 前基线（`find ...*.md` 总量、99_归档 量、archive/fulltext 量）；
2. 按清单同目录 rename 热区正式 .md（40 个）；
3. 创建新的 `L0_INDEX_START_HERE_地基启动索引_v1.0.md`（原始内容迁移）；
4. 将旧 `START_HERE_地基启动索引.md` 改为 redirect（2 行）；
5. 更新 7 个热区索引/README 类文件中的路径引用；
6. 新增 `99_归档/RENAME_MANIFEST_20260609.md`；
7. **不触碰** JSON、脚本、数据、生产入口、Git/GitHub；
8. 输出 G4 自检报告（按 §六 14 项标准逐条输出）；
9. 暂停，等待用户确认进入 G5。

### G5/G6

- G4 PASS → 用户确认 → **旧影 G5 独立复查**
- G5 PASS → 用户确认 → **腰子 G6（或跳过说明）**
- 用户确认收口

---

## 八、禁止状态声明

- **阿黑**：G2 准入单已落盘，**不得进入 G3**。
- **红结**：**不得实施** rename。
- **旧影**：**不得提前** G5。
- **腰子**：**不得被代签**。
- **执行模型**：**不得替代**项目角色确认。

---

*创建日期：2026-06-09*
*责任角色：阿黑*
*流程：F-RUNBOOK + F-FIX + F-GATE*
