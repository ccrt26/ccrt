# G5 旧影复查报告 (修正 v1.0.1)

| 项目 | 内容 |
|:-----|:-----|
| 任务名称 | G3-QINGSHAN-SOURCE-SELECTION-FIX-v1.0.1 |
| 审计阶段 | G5 旧影复查 |
| 报告版本 | v1.0.1 |
| 审计人 | 旧影 |
| 审计日期 | 2026-06-11 |
| 流程类型 | F-ARCH |
| 前驱 | G4 自检 ✅ PASS |
| 修正范围 | validation 字段语义 + G6 放行人口径 |

---

## 复查主题

### 1. 字段语义是否已修复？

**结论：✅ 已修复。**

| 检查项 | 修复前 | 修复后 |
|:-------|:-------|:-------|
| bad_direct_application_policy | `[{"principle_has_applied": true, "gate_005_exists": true}]`（PASS 证据对象） | `[]`（空数组 = 无违规） |
| direct_application_policy_ok | 无此字段 | `true`（布尔语义明确） |
| 程序可解析性 | 差：需检查数组内容判断是否合规 | 好：`[]` + `true` 直接判断 |

**评估：** 修复后的输出符合"bad 字段只放 FAIL 证据、good 字段用 bool 表示"的规范。代码路径清晰，不会误读。

### 2. G6 放行人口径是否修正？

**结论：✅ 已修正。**

- 原 G6 放行记录（v1.0）误将审计人写为"新安"，放行签名写为"G6 新安放行"
- G6 是最终放行归档门，应由腰子（业务负责人）签署，而非新安（质量工程师）
- v1.0.1 的 G6 修正为腰子口径，不再出现"新安放行"

### 3. source selection 机制是否仍然不是死白名单？

**结论：✅ 保持不变。**

- `selection_principle` 未修改，"候选池不是白名单"声明有效
- 每个 preferred source 的 `not_exclusive: true` 保持
- 新增来源通过 must_have_gates 即可进入，不依赖候选池修改
- policy JSON 未修改，机制完整

### 4. 是否符合青山职责？

**结论：✅ 符合。**

本次修正不涉及职责范围变更。青山职责边界仍由 policy `scope` 定义。

### 5. 是否建议通过？

**结论：✅ 建议通过。**

两项修复均已完成：
1. validation 字段语义已规范，不再用 `bad_*` 字段承载 PASS 证据
2. G6 放行人已修正为腰子口径

---

## 综合评估

| 复查维度 | 结果 |
|:---------|:-----|
| 字段语义修复 | ✅ PASS |
| G6 放行人口径修正 | ✅ PASS |
| 死白名单防御 | ✅ 有效保持 |
| 青山职责匹配 | ✅ PASS |
| 下一阶段建议 | ✅ 建议进入文献质量评分阶段 |

**G5 结论：✅ PASS — 修正内容合规，建议进入修正后 G6 放行。**
