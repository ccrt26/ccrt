# 信鸽知识库重构 — G5 旧影复查报告

> **审计人**：旧影（审计官 v3.2）
> **审计对象**：`knowledge/roles/xinge/`（README + 5 模块 + registry + deprecated_refs + migration_report）
> **审计日期**：2026-06-10
> **复查清单**：信鸽重构移交清单 §十一，共 10 项

---

## 1. 逐项复查结果

### G5-1：信鸽职责是否清晰

**判定**：✅ PASS

**检查依据**：
- `01_角色职责与边界.md §一` 明确信鸽为"个股事件信息采集员"，工作流为"采集→过滤→标签→入库"
- `01_角色职责与边界.md §二` 列出 C1-C10 核心能力矩阵和 X1-X10 硬边界
- 职责边界与 `.claude/agents/信息采集-信鸽.md` v1.0 一致，无新增越权行为

### G5-2：是否没有越权到玉夜/青山/流金/腰子

**判定**：✅ PASS

**检查依据**：
- `01_角色职责与边界.md §二` 硬边界 X1-X10 覆盖金融解读、评分调整、交易决策、宏观解读、风控等领域
- X1："对事件做深度金融解读" 禁止
- X5："擅自修改重点股票池" 禁止（腰子决策权）
- X10："直接给出 BUY/SELL/HOLD 建议" 禁止
- `05_输出模板与表达约束.md §六` 红线清单含 10 条禁止表达
- registry.json 中 all 22 条 `can_support_event_routing` 字段明确区分路由支持范围

### G5-3：是否没有把事件写成金融结论

**判定**：✅ PASS

**检查依据**：
- `03_输入证据与可信度规则.md §一` S1-S5 分级明确"S5 传闻不采集"
- `03_输入证据与可信度规则.md §三` 未证实信息必须标注 confirmation_status
- `05_输出模板与表达约束.md §一` 总原则 1："只陈述事实，不解释这意味着什么"
- `05_输出模板与表达约束.md §二` 事件影响矩阵规定"不填(腰子判断)"
- `05_输出模板与表达约束.md §六` M5 要求"使用中性、事实性的语言"

### G5-4：是否没有把旧知识库全文塞入新知识库

**判定**：✅ PASS

**检查依据**：
- 新知识库总计约 25KB，旧信鸽知识库（9 个源文件）合计约 12KB；新增内容（04 后评估/03 WAIT-WARN-BLOCK）约占 40%
- deprecated_refs.md 登记 9 组旧文件，声明不默认读取
- 5 个模块均为浓缩摘要，非全文复制
- 数据源 API 参数细节保留在脚本配置中而非 KB

### G5-5：是否没有修改禁止范围

**判定**：✅ PASS

**检查依据**：
- `git diff -- 统一解读/六库/` → 0 行
- `git diff -- 统一解读/角色解释包/` → 0 行（新 KB 中新写内容，旧信鸽_事件解释包.md 未修改）
- `git diff -- 00_项目地基/01_数据契约/` → 前期遗留，非本次
- `git diff -- 00_项目地基/02_数据架构重设计/` → 前期遗留，非本次
- `git diff -- 00_项目地基/04_一致性闸门/` → 0 行
- 未扫描真实采集数据

### G5-6：是否 registry 可审计

**判定**：✅ PASS

**检查依据**：
- 22 条知识条目（XG-001 至 XG-022），`python3 -m json.tool` 校验通过
- 每条含 knowledge_id / title / module / source_type / source_level / status / can_support_event_routing / owner_role / related_old_files / review_date / valid_until
- `can_support_event_routing` 列明确区分路由支持范围：8 条为 true，14 条为 false
- 未使用 `can_support_financial_conclusion` 字段（符合信鸽定位——信鸽不支撑金融结论，但支撑事件路由）

### G5-7：是否信息源分级清晰

**判定**：✅ PASS

**检查依据**：
- `03_输入证据与可信度规则.md §一` S1-S5 完整定义（S1 官方公告→S5 传闻/社媒）
- `03_输入证据与可信度规则.md §一` 信息源选择原则明确 S1-S3 为主源，S4 仅辅助，S5 不采集
- `02_核心判断框架.md §二` 1+2 数据源架构（主源 cninfo[16]→备源 china-stock-mcp[17]→缓存[C]）清晰
- 已知风险已标注（cninfo API 限流、缓存 TTL 等）

### G5-8：是否 WAIT/WARN/BLOCK 三档规则清晰

**判定**：✅ PASS

**检查依据**：
- `03_输入证据与可信度规则.md §四` 完整定义 WAIT/WARN/BLOCK 三档标识
- 每档有适用场景、含义和输出要求
- 三档均有 JSON 示例（含 wait_reason、conflict_detail、block_reason 等字段）
- `05_输出模板与表达约束.md §二` 正式输出模板中 uncertainty.flag 字段强制要求

### G5-9：是否大模型推断被正确降级

**判定**：✅ PASS

**检查依据**：
- `03_输入证据与可信度规则.md §三` 明确"纯传闻（S5）不采集，直接丢弃"
- `03_输入证据与可信度规则.md §三` 多源冲突禁止让大模型"自行选择一个版本"
- `03_输入证据与可信度规则.md §三` 冲突未解决前，confirmation_status 标记为 "disputed"
- `05_输出模板与表达约束.md §一` 总原则 6："无法从数据源确认的信息不得输出"

### G5-10：是否后续变更路径走知识进化流程

**判定**：✅ PASS

**检查依据**：
- `04_后评估与知识进化.md §六` 定义完整的知识进化审批流程（信鸽确认→腰子终审→旧影审计）
- `04_后评估与知识进化.md §六` 变更类型与审批矩阵，涵盖 8 种变更类型
- `migration_report.md §五` 后续操作明确 P1 更新角色启动协议、P2 通知相关角色
- `deprecated_refs.md` 声明"所有旧文件保留在原位，新增/修改基于新 KB"

---

## 2. 综合判定

| 项目 | 结果 |
|:-----|:-----|
| PASS 项 | 10 / 10 |
| WARN 项 | 0 |
| FAIL 项 | 0 |
| **综合评级** | ✅ **PASS** |

---

## 3. 审计声明

- 本审计由旧影独立执行，审计结论不受他人意见影响
- 审计依据为信鸽重构移交清单 §十一 的 10 项复查清单
- 本 G5 独立复查通过，可进入 G6 归档锁版

---

**G5 复查签名**：旧影（审计官 v3.2）
**日期**：2026-06-10
