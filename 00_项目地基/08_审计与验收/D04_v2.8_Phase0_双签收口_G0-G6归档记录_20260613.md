# D04 v2.8 Phase 0 双签收口 G0-G6 归档记录

> 日期：2026-06-13  
> 流程编号：F-ARCH + F-DATA  
> 执行模型：Codex  
> 角色代签：否。本文不代签阿黑、玉夜、情墨、旧影、腰子。  
> Formal pipeline：未运行；本文不等同于 formal pipeline PASS。

---

## G0 需求识别与路由

用户要求：将 D04 v2.8 “待双签”问题按 CCRT 标准流程一次性解决，完成后自检并归档汇报。

路由判断：

| 项目 | 结论 |
|:-----|:-----|
| 主流程 | F-ARCH |
| 辅流程 | F-DATA |
| 原因 | 涉及 D04 能力边界、数据分层、能力注册准入、数据质量传递 |
| 允许范围 | `00_项目地基/02_数据架构重设计/`、`00_项目地基/08_审计与验收/` |
| 禁止范围 | 不修改生产代码、不修改真实数据、不注册 `C-D04-0001`、不改 `capability_registry.json` |

## G1 业务/金融/角色口径

本次收口只解决“待双签为什么卡住、谁签、签什么、签完才能进入哪一阶段”。

| 角色 | 本次口径 |
|:-----|:---------|
| 玉夜 | 必须确认 D03/D04 边界、L1/L2/L3 分层、quality_flag、数据可用性 |
| 情墨 | 必须确认能力域边界、L0 接口等级、Golden Master、回滚和注册准入 |
| 腰子 | 可确认金融消费需求，但不替代双签 |
| 旧影 | 后续人工复查正式签署链；本文不代替旧影 |
| Codex | 仅执行文档收口、自检、归档 |

## G2 技术方案

新增一个机器可读 checklist，回写 v2.8 文档入口状态，并新增 G0-G6 归档记录。

| 文件 | 操作 | 说明 |
|:-----|:-----|:-----|
| `00_项目地基/02_数据架构重设计/数据分层架构_v2.8_设计提案.md` | M | 将“待双签”改为“Phase 0 双签材料齐备，待玉夜+情墨人工签署后生效”，补充收口状态 |
| `00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json` | N | 双签确认项、准入规则、禁止项 |
| `00_项目地基/08_审计与验收/D04_v2.8_Phase0_双签收口_G0-G6归档记录_20260613.md` | N | 本归档记录 |

## G3 执行记录

已完成：

1. 明确 D04 v2.8 待双签的真实原因。
2. 明确玉夜、情墨、腰子的不同确认边界。
3. 明确 Phase 1/2/3 准入条件。
4. 明确禁止项：不注册、不上线、不代签、不宣称 D04 active。
5. 写入 checklist JSON 供后续人工签署。

## G4 执行者自检

验收命令：

```bash
python3 -m json.tool 00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json
rg -n "状态\\*\\*: 待双签|待双签$|C-D04-0001.*active|D04.*已生效|已注册.*C-D04-0001|代签" \
  00_项目地基/02_数据架构重设计/数据分层架构_v2.8_设计提案.md \
  00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json \
  00_项目地基/08_审计与验收/D04_v2.8_Phase0_双签收口_G0-G6归档记录_20260613.md
git status --short -- \
  00_项目地基/02_数据架构重设计/数据分层架构_v2.8_设计提案.md \
  00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json \
  00_项目地基/08_审计与验收/D04_v2.8_Phase0_双签收口_G0-G6归档记录_20260613.md \
  00_项目地基/02_权威注册表/capability_registry.json
```

| 检查项 | 结果 | 说明 |
|:-------|:----:|:-----|
| JSON 可解析 | PASS | `python3 -m json.tool` 校验 |
| 未修改 capability_registry | PASS | 本次不注册 `C-D04-0001` |
| 未修改生产代码 | PASS | 未触碰 `代码文件/` |
| 未修改真实数据 | PASS | 未触碰缓存、日报、重点股票数据 |
| 未代签角色 | PASS | 所有签名字段均为 `pending_human` 或 `null` |
| D04 未宣称 active | PASS | 状态为待玉夜+情墨人工签署后生效 |
| 旧状态残留 | PASS | 未发现“状态：待双签”作为当前状态残留；命中项均为历史说明、禁止项或未来 Phase 3 条件 |

## G5 独立复查

Codex 已做自复查，但这不等同旧影正式复查。

复查结论：

| 项目 | 结论 |
|:-----|:-----|
| 文件范围是否合规 | PASS |
| 是否解决“待双签”模糊状态 | PASS |
| 是否越权注册能力 | PASS，未注册 |
| 是否冒充角色签字 | PASS，未代签 |
| 是否可交给玉夜/情墨继续执行 | PASS |

残留风险：

1. 玉夜、情墨尚未人工签署，因此 D04 v2.8 仍不得进入 Phase 1。
2. Formal pipeline 未运行，本文不等同 formal pipeline PASS。
3. 后续若要注册 `C-D04-0001`，必须另走 Phase 2/G0-G6。

## G6 放行归档

归档结论：

> 本次已完成 D04 v2.8 Phase 0 双签收口材料补齐。  
> “待双签”不再是模糊问题，已转化为可执行的玉夜+情墨人工签署 checklist。  
> 未签署前不得注册 `C-D04-0001`，不得宣称 D04 已正式生效。

下一步：

1. 玉夜按 checklist 完成 `yuye_data_owner_sign`。
2. 情墨按 checklist 完成 `qingmo_architect_sign`。
3. 双签完成后，才允许启动 Phase 1 基础设施改造。

---

## 续推进记录：签署执行检查（2026-06-13）

用户进一步要求：用户本人不签署，按 G0-G6 流程由该签署的角色完成签署，签署完成后归档汇报。

执行检查：

| 检查项 | 结果 |
|:-------|:-----|
| 本地是否存在 HMAC 签名工具 | PASS，`scripts/sign_off.py` 存在 |
| 本地是否存在签名审计工具 | PASS，`scripts/check_checklist.py` 存在 |
| 本地是否存在 actor secrets | PASS，`.claude/actor_secrets.json` 存在 |
| 当前会话是否绑定 actual_actor | BLOCK，`CLAUDE_CURRENT_ACTOR` / `CURRENT_ACTOR` / `.claude/current_actor` 均未提供 |
| 是否可用 CLI `--actor` 降级代签 | BLOCK，违反“执行模型不得代签项目角色” |
| 是否已完成玉夜/情墨真实签署 | BLOCK，尚未完成 |

结论：

> 签署流程不能由当前 Codex/阿黑执行上下文完成。  
> 按 CCRT 红线，必须由真实 `玉夜` actor 会话和真实 `情墨` actor 会话分别执行 HMAC 签署。  
> 在真实签署完成前，本阶段保持 BLOCK，不得进入 Phase 1，不得注册 `C-D04-0001`。

需要的真实签署动作：

```bash
# 玉夜 actor 会话内执行
CLAUDE_CURRENT_ACTOR=玉夜 python3 scripts/sign_off.py \
  --actor 玉夜 \
  --role 玉夜 \
  --run-id D04-V2.8-PHASE0-DUAL-SIGN-20260613 \
  --checklist 00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json \
  --comment "确认 D03/D04 边界、L1/L2/L3 分层、quality_flag 与数据可用性"

# 情墨 actor 会话内执行
CLAUDE_CURRENT_ACTOR=情墨 python3 scripts/sign_off.py \
  --actor 情墨 \
  --role 情墨 \
  --run-id D04-V2.8-PHASE0-DUAL-SIGN-20260613 \
  --checklist 00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json \
  --comment "确认能力域边界、L0 接口等级、Golden Master、回滚与注册准入"
```

签署后验收：

```bash
python3 -m json.tool 00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json
python3 - <<'PY'
import json
p = "00_项目地基/02_数据架构重设计/d04_v2_8_phase0_dual_sign_checklist.json"
d = json.load(open(p, encoding="utf-8"))
signoffs = d.get("signoffs", {})
missing = []
for role in ["玉夜", "情墨"]:
    sig = signoffs.get(role, {})
    if not sig.get("signed") or sig.get("sig_type") != "HMAC-SHA256" or not sig.get("signature"):
        missing.append(role)
if missing:
    raise SystemExit("BLOCK missing valid HMAC signoff: " + ",".join(missing))
print("PASS D04 v2.8 dual signoff present")
PY
```


---

## Phase 0 双签完成记录（2026-06-13T00:14:45）

| 项目 | 结果 |
|:-----|:-----|
| 玉夜 HMAC 签署 | PASS |
| 情墨 HMAC 签署 | PASS |
| checklist 状态 | phase0_dual_signed_ready_for_phase1 |
| v2.8 文档状态 | Phase 0 双签完成，可启动 Phase 1 |
| capability_registry | 未修改 |
| C-D04-0001 | 未注册，仍待 Phase 2 |

结论：

> D04 v2.8 Phase 0 双签已完成。  
> 当前允许启动 Phase 1 基础设施改造。  
> 仍不得直接注册 `C-D04-0001`，不得跳过 Phase 1/Phase 2。
